<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Teams per group
    |--------------------------------------------------------------------------
    | Number of teams assigned to each group during a tournament draw.
    | Also equals the number of pots required and the exact team count
    | needed for fixture generation (double round-robin).
    */
    'teams_per_group' => 4,
];
